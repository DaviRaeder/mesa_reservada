<h1 class="text-3xl font-semibold text-gray-800 mb-6">Usuário: <%= @user.email %></h1>

<p><strong>Email:</strong> <%= @user.email %></p>

<div class="mt-6">
  <%= link_to 'Editar', edit_admin_user_path(@user), class: 'bg-yellow-500 text-white py-2 px-4 rounded-md hover:bg-yellow-600' %>
  <%= link_to 'Voltar', admin_users_path, class: 'text-blue-500 hover:text-blue-600 ml-4' %>
</div>
